apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: minio-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/minio/minio-helm-values
  type: Opaque
