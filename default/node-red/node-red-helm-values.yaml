apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: node-red-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/node-red/node-red-helm-values
  type: Opaque
