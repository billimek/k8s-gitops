apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: goldilocks-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/goldilocks/goldilocks-helm-values
  type: Opaque
