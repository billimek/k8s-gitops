apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: hubot-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/hubot/hubot-helm-values
  type: Opaque
