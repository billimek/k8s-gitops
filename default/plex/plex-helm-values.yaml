apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: plex-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/plex/plex-helm-values
  type: Opaque
