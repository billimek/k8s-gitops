apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: unifi-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/unifi/unifi-helm-values
  type: Opaque
