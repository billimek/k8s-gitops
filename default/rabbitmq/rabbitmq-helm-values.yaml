apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: rabbitmq-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/rabbitmq/rabbitmq-helm-values
  type: Opaque
