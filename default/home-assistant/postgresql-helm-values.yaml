apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: postgresql-helm-values
  namespace: default
spec:
  keys:
    - values.yaml
  path: secrets/default/home-assistant/postgresql-helm-values
  type: Opaque
