---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # ┌──────────────────┐
  # │ rook-ceph        │
  # └──────────────────┘
  # NOTE: Kept as safety backup - also installed via helmfile bootstrap
  # renovate: datasource=github-releases depName=rook/rook
  # https://github.com/rook/rook/raw/v1.18.8/deploy/examples/crds.yaml
  - ./vendor/rook_rook/crds.yaml

