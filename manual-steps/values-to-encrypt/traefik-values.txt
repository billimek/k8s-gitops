acme:
  email: $EMAIL
  domains:
    domainsList:
      - main: "*.$DOMAIN"
      - sans:
        - "$DOMAIN"
      - main: "*.k.$DOMAIN"
  dnsProvider:
    cloudflare:
      CLOUDFLARE_EMAIL: $EMAIL
      CLOUDFLARE_API_KEY: "$CF_API_KEY"
dashboard:
  domain: t.k.$DOMAIN
  auth :
    basic:
       $TRAEFIK_AUTH