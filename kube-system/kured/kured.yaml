---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kured
  namespace: kube-system
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://weaveworks.github.io/kured
      chart: kured
      version: 2.3.2
      sourceRef:
        kind: HelmRepository
        name: weaveworks-kured-charts
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: raspbernetes/kured
      tag: 1.6.1
      pullPolicy: Always # the 1.5.1 tag was reused to fix a bug, forcing pull for this version only
    configuration:
      startTime: "0:00"
      endTime: "4:00"
      timeZone: "America/New_York"
    tolerations:
    - key: "arm"
      operator: "Exists"
    - key: "node-role.kubernetes.io/master"
      operator: "Exists"
    metrics:
      create: true
  valuesFrom:
  - kind: Secret
    name: "kured-helm-values"
    optional: false
