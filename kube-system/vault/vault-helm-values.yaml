apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: vault-helm-values
  namespace: kube-system
spec:
  keys:
    - values.yaml
  path: secrets/kube-system/vault/vault-helm-values
  type: Opaque
