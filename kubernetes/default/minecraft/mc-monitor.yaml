---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/servicemonitor_v1.json
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: minecraft-router
  namespace: default
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: minecraft-router
      app.kubernetes.io/service: minecraft-router-api
  endpoints:
    - port: api
      interval: 60s
      scrapeTimeout: 30s
      path: /metrics
---
# yaml-language-server: $schema=https://k8s-schemas.bjw-s.dev/grafana.integreatly.org/grafanadashboard_v1beta1.json
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: minecraft-servers
  namespace: default
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  allowCrossNamespaceImport: true
  folder: apps
  json: |
    {
      "title": "Minecraft Servers",
      "uid": "minecraft-servers",
      "tags": ["minecraft"],
      "refresh": "1m",
      "time": {"from": "now-6h", "to": "now"},
      "panels": [
        {
          "id": 1,
          "title": "Active Connections per Server",
          "type": "stat",
          "gridPos": {"x": 0, "y": 0, "w": 24, "h": 4},
          "fieldConfig": {
            "defaults": {
              "thresholds": {"steps": [{"color": "blue", "value": 0}, {"color": "green", "value": 1}]},
              "mappings": []
            }
          },
          "options": {"reduceOptions": {"calcs": ["lastNotNull"]}, "colorMode": "background"},
          "targets": [
            {
              "expr": "mc_router_server_active_connections",
              "legendFormat": "{{server_address}}"
            }
          ]
        },
        {
          "id": 2,
          "title": "Players Online",
          "type": "stat",
          "gridPos": {"x": 0, "y": 4, "w": 12, "h": 4},
          "fieldConfig": {
            "defaults": {"thresholds": {"steps": [{"color": "blue", "value": 0}]}}
          },
          "options": {"reduceOptions": {"calcs": ["lastNotNull"]}, "colorMode": "value"},
          "targets": [
            {
              "expr": "sum by (server_address) (mc_router_server_active_player)",
              "legendFormat": "{{server_address}}"
            }
          ]
        },
        {
          "id": 3,
          "title": "Total Active Connections",
          "type": "stat",
          "gridPos": {"x": 12, "y": 4, "w": 12, "h": 4},
          "fieldConfig": {
            "defaults": {"thresholds": {"steps": [{"color": "blue", "value": 0}]}}
          },
          "options": {"reduceOptions": {"calcs": ["lastNotNull"]}, "colorMode": "value"},
          "targets": [
            {
              "expr": "mc_router_active_connections",
              "legendFormat": "total"
            }
          ]
        },
        {
          "id": 4,
          "title": "Player Logins (last 1h)",
          "type": "timeseries",
          "gridPos": {"x": 0, "y": 8, "w": 24, "h": 8},
          "targets": [
            {
              "expr": "increase(mc_router_server_logins[1h])",
              "legendFormat": "{{player_name}} -> {{server_address}}"
            }
          ]
        },
        {
          "id": 5,
          "title": "Network Throughput",
          "type": "timeseries",
          "gridPos": {"x": 0, "y": 16, "w": 24, "h": 8},
          "fieldConfig": {"defaults": {"unit": "Bps"}},
          "targets": [
            {
              "expr": "rate(mc_router_bytes[5m])",
              "legendFormat": "bytes/s"
            }
          ]
        }
      ],
      "schemaVersion": 36
    }
